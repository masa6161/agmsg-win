# PR 作成先ポリシー

PR は常に `masa6161/agmsg-win` (origin) に対して作成すること。

- fork元 upstream に PR を作成してはならない
- `gh pr create` 実行時は `--repo masa6161/agmsg-win` を明示指定する
- ユーザーから明示的にフォーク元への PR を指示された場合のみ、upstream を対象にしてよい
